# Python >= 3.10 (the code uses PEP 604 union syntax, e.g. `str | None`).
#
# Versions are lower bounds: the code was developed against these or later.
# Pin them exactly if you need a bit-for-bit reproduction of the published runs.

numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
statsmodels>=0.14
matplotlib>=3.7
joblib>=1.3

# Gradient boosting and the neural baseline (IPSS pipeline only).
catboost>=1.2
xgboost>=2.0
torch>=2.0

# Hyperparameter search and model explanation (IPSS pipeline only).
optuna>=3.4
shap>=0.44

# Markdown table rendering used by several .to_markdown() calls.
tabulate>=0.9
